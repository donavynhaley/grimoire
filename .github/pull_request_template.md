## What this changes

<!-- One idea per pull request. Say what changed and why, the way the commit log
     already reads. If this closes a finding against docs/coding-standards.md,
     name the ID it closes. -->

## How it was verified

<!-- Delete the lines that do not apply. -->

- [ ] `npm run check` — types and Biome
- [ ] `npm test` — the unit and integration suite, with a test for this change
- [ ] `npm run test:e2e` — run locally for interface work
- [ ] `npm run verify:mcp` — run for changes to the MCP package or the HTTP API

## Canon

- [ ] `docs/architecture.md`, `docs/coding-standards.md`, and `docs/ui-standards.md`
      still describe the truth after this change, or are updated in this pull
      request (DOC-3)
